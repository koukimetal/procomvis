import {SolverBase} from "shared/solve";

export class Solver extends SolverBase<{}, {}> {
    parse(text: string) {
        return {};
    }
    solve(input: {}) {
        return {};
    }
    print(output: {}) {
    }
}